[koen.klinke.rs](https://koen.klinke.rs)

## Editing

- `index.md` is the source of truth. `index.html` is generated — don't edit it by hand.
- After editing: `uv run build.py`, then commit both files.
